[project]
name = "it-helpdesk-openenv"
version = "1.0.0"
description = "IT Helpdesk Incident Triage OpenEnv backend"
requires-python = ">=3.10"

dependencies = [
    "fastapi",
    "uvicorn",
    "pydantic",
    "jinja2",
    "openenv-core>=0.2.0"
]

[project.scripts]
server = "server.app:main"